# =============================================================================
# ACR-QA — Code Review Product Workflow
# =============================================================================
# PURPOSE: Analyzes Pull Request diffs, posts AI-powered review comments,
#          and enforces quality gates.
# TRIGGERS:
#   - Auto: Every PR (opened, updated, reopened)
#   - Manual: Comment "acr-qa review" on any PR
# =============================================================================

name: ACR-QA Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: write
  issues: write
  security-events: write

jobs:
  # ─── Job 1: Auto-trigger on PR events ─────────────────────────────────
  analyze-pr:
    name: Analyze Pull Request
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event_name == 'pull_request'

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: acrqa_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup ACR-QA environment
        uses: ./.github/actions/acr-qa-setup
        with:
          db-name: acrqa_test
          db-password: postgres

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v47
        with:
          files: |
            **/*.py
            **/*.js
            **/*.ts
            **/*.go

      - name: Run ACR-QA Analysis
        if: steps.changed-files.outputs.any_changed == 'true'
        id: analysis
        continue-on-error: true
        env:
          GROQ_API_KEY_1: ${{ secrets.GROQ_API_KEY_1 }}
          GROQ_API_KEY_2: ${{ secrets.GROQ_API_KEY_2 }}
          GROQ_API_KEY_3: ${{ secrets.GROQ_API_KEY_3 }}
          GROQ_API_KEY_4: ${{ secrets.GROQ_API_KEY_4 }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: acrqa_test
          DB_USER: postgres
          DB_PASSWORD: postgres
        run: |
          set -o pipefail
          mkdir -p /tmp/pr-files
          echo "${{ steps.changed-files.outputs.all_changed_files }}" | tr ' ' '\n' > /tmp/changed_files.txt
          while IFS= read -r file; do
            if [ -f "$file" ]; then
              mkdir -p "/tmp/pr-files/$(dirname "$file")"
              cp "$file" "/tmp/pr-files/$file"
            fi
          done < /tmp/changed_files.txt
          python3 CORE/main.py \
            --target-dir /tmp/pr-files \
            --repo-name "${{ github.repository }}" \
            --pr-number ${{ github.event.pull_request.number }} \
            --limit 100 | tee /tmp/analysis_output.txt
          grep "Run ID:" /tmp/analysis_output.txt | awk '{print $NF}' > /tmp/acr_run_id.txt

      - name: Export Confirmed-Only SARIF (96.4% precision gate)
        # Only export Confirmed Tier findings to GHAS — zero noise for the Security tab
        if: steps.changed-files.outputs.any_changed == 'true'
        continue-on-error: true
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: acrqa_test
          DB_USER: postgres
          DB_PASSWORD: postgres
        run: |
          RUN_ID=$(cat /tmp/acr_run_id.txt 2>/dev/null || echo "")
          if [ -n "$RUN_ID" ]; then
            python3 scripts/export_sarif.py \
              --run-id "$RUN_ID" \
              --output DATA/outputs/findings.sarif \
              --confirmed-only
          fi

      - name: Upload Confirmed-Only SARIF to GitHub Security Tab
        if: steps.changed-files.outputs.any_changed == 'true' && hashFiles('DATA/outputs/findings.sarif') != ''
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: DATA/outputs/findings.sarif
          category: acrqa-confirmed-tier

      - name: ACR-QA Confirmed Tier Merge Gate
        # Fails the check if any Confirmed Tier findings exist in the PR diff.
        # These are exploit-verified, 96.4%-precise findings — safe to auto-block on.
        if: steps.changed-files.outputs.any_changed == 'true'
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: acrqa_test
          DB_USER: postgres
          DB_PASSWORD: postgres
          RUN_ID_ENV: ${{ steps.analysis.outputs.run_id }}
        run: |
          RUN_ID=$(cat /tmp/acr_run_id.txt 2>/dev/null || echo "")
          if [ -z "$RUN_ID" ]; then
            echo "::notice title=ACR-QA::No run ID found — skipping merge gate"
            exit 0
          fi
          # Count Confirmed Tier findings in this run
          CONFIRMED=$(python3 - <<'PYEOF'
          import sys, os
          sys.path.insert(0, ".")
          from DATABASE.database import Database
          from CORE.engines.confirmed_tier import ConfirmedTierEngine
          db = Database()
          run_id = int(os.environ.get("RUN_ID_ENV", 0))
          if not run_id:
              print(0)
              sys.exit(0)
          findings = db.get_findings(run_id=run_id, limit=10000)
          engine = ConfirmedTierEngine()
          count = sum(
              1 for f in findings
              if engine.classify({**f, "file": f.get("file_path", "")}).in_confirmed_tier
          )
          print(count)
          PYEOF
          )
          echo "::notice title=ACR-QA Confirmed Tier::$CONFIRMED confirmed finding(s) (96.4% precision gate)"
          if [ "$CONFIRMED" -gt 0 ]; then
            echo "::error title=ACR-QA Merge Gate::$CONFIRMED Confirmed Tier finding(s) must be fixed before merge. These findings have been exploit-verified or passed the 96.4%-precision gate."
            exit 1
          fi
          echo "✅ Confirmed Tier gate passed — 0 high-confidence findings. Safe to merge."

      - name: Post PR Comments
        if: steps.changed-files.outputs.any_changed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: acrqa_test
          DB_USER: postgres
          DB_PASSWORD: postgres
        run: |
          python3 scripts/post_pr_comments.py \
            --repo "${{ github.repository }}" \
            --pr-number ${{ github.event.pull_request.number }} \
            --run-id-file /tmp/acr_run_id.txt

      - name: Create Autofix PR
        if: steps.changed-files.outputs.any_changed == 'true' && steps.analysis.outcome != 'cancelled'
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: acrqa_test
          DB_USER: postgres
          DB_PASSWORD: postgres
        run: |
          RUN_ID=$(cat /tmp/acr_run_id.txt 2>/dev/null || echo "")
          if [ -z "$RUN_ID" ]; then
            echo "No run ID found — skipping autofix PR"
            exit 0
          fi
          python3 scripts/create_fix_pr.py \
            --run-id "$RUN_ID" \
            --repo "${{ github.repository }}" \
            --base-branch "${{ github.event.pull_request.base.ref }}" \
            || echo "Autofix PR skipped (no validated fixes or PR already exists)"

      - name: Post Quality Gate Summary
        if: steps.changed-files.outputs.any_changed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: acrqa_test
          DB_USER: postgres
          DB_PASSWORD: postgres
        run: |
          python3 scripts/post_gate_comment.py \
            --repo "${{ github.repository }}" \
            --pr-number ${{ github.event.pull_request.number }} \
            --run-id-file /tmp/acr_run_id.txt

      - name: Post PR Risk Score
        if: steps.changed-files.outputs.any_changed == 'true'
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: acrqa_test
          DB_USER: postgres
          DB_PASSWORD: postgres
        run: |
          python3 scripts/post_pr_risk_comment.py \
            --repo "${{ github.repository }}" \
            --pr-number ${{ github.event.pull_request.number }} \
            --run-id-file /tmp/acr_run_id.txt \
            --base-ref "origin/${{ github.event.pull_request.base.ref }}"

      - name: Enforce Quality Gate
        if: steps.analysis.outcome == 'failure'
        run: |
          python3 -c "
          import yaml, sys, os
          cfg = yaml.safe_load(open('.acrqa.yml')) if os.path.exists('.acrqa.yml') else {}
          mode = cfg.get('quality_gate', {}).get('mode', 'block')
          print(f'Gate mode: {mode}')
          sys.exit(0 if mode == 'warn' else 1)
          "

  # ─── Job 2: Manual trigger via "acr-qa review" comment ────────────────
  manual-trigger:
    name: Manual Review (comment trigger)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: |
      github.event_name == 'issue_comment' &&
      github.event.issue.pull_request &&
      contains(github.event.comment.body, 'acr-qa review')

    permissions:
      contents: read
      pull-requests: write
      issues: write
      security-events: write

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: acrqa_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: React to comment (eyes)
        uses: peter-evans/create-or-update-comment@v5
        with:
          comment-id: ${{ github.event.comment.id }}
          reactions: 'eyes'

      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: refs/pull/${{ github.event.issue.number }}/head

      - name: Setup ACR-QA environment
        uses: ./.github/actions/acr-qa-setup
        with:
          db-name: acrqa_test
          db-password: postgres

      - name: Run ACR-QA Analysis (Full Scan)
        id: analysis
        continue-on-error: true
        env:
          GROQ_API_KEY_1: ${{ secrets.GROQ_API_KEY_1 }}
          GROQ_API_KEY_2: ${{ secrets.GROQ_API_KEY_2 }}
          GROQ_API_KEY_3: ${{ secrets.GROQ_API_KEY_3 }}
          GROQ_API_KEY_4: ${{ secrets.GROQ_API_KEY_4 }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: acrqa_test
          DB_USER: postgres
          DB_PASSWORD: postgres
        run: |
          set -o pipefail
          python3 CORE/main.py \
            --target-dir . \
            --repo-name "${{ github.repository }}" \
            --pr-number ${{ github.event.issue.number }} \
            --limit 100 | tee /tmp/analysis_output.txt
          grep "Run ID:" /tmp/analysis_output.txt | awk '{print $NF}' > /tmp/acr_run_id.txt

      - name: Upload SARIF findings to Security Tab
        if: hashFiles('DATA/outputs/findings.sarif') != ''
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: DATA/outputs/findings.sarif

      - name: Post PR Comments
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: acrqa_test
          DB_USER: postgres
          DB_PASSWORD: postgres
        run: |
          python3 scripts/post_pr_comments.py \
            --repo "${{ github.repository }}" \
            --pr-number ${{ github.event.issue.number }} \
            --run-id-file /tmp/acr_run_id.txt

      - name: Create Autofix PR
        if: steps.analysis.outcome != 'cancelled'
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: acrqa_test
          DB_USER: postgres
          DB_PASSWORD: postgres
        run: |
          RUN_ID=$(cat /tmp/acr_run_id.txt 2>/dev/null || echo "")
          if [ -z "$RUN_ID" ]; then
            echo "No run ID found — skipping autofix PR"
            exit 0
          fi
          python3 scripts/create_fix_pr.py \
            --run-id "$RUN_ID" \
            --repo "${{ github.repository }}" \
            --base-branch "main" \
            || echo "Autofix PR skipped"

      - name: Post Quality Gate Summary
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: acrqa_test
          DB_USER: postgres
          DB_PASSWORD: postgres
        run: |
          python3 scripts/post_gate_comment.py \
            --repo "${{ github.repository }}" \
            --pr-number ${{ github.event.issue.number }} \
            --run-id-file /tmp/acr_run_id.txt

      - name: Post PR Risk Score
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: acrqa_test
          DB_USER: postgres
          DB_PASSWORD: postgres
        run: |
          python3 scripts/post_pr_risk_comment.py \
            --repo "${{ github.repository }}" \
            --pr-number ${{ github.event.issue.number }} \
            --run-id-file /tmp/acr_run_id.txt

      - name: Enforce Quality Gate
        if: steps.analysis.outcome == 'failure'
        run: |
          python3 -c "
          import yaml, sys, os
          cfg = yaml.safe_load(open('.acrqa.yml')) if os.path.exists('.acrqa.yml') else {}
          mode = cfg.get('quality_gate', {}).get('mode', 'block')
          print(f'Gate mode: {mode}')
          sys.exit(0 if mode == 'warn' else 1)
          "

      - name: React to completion
        if: always()
        uses: peter-evans/create-or-update-comment@v5
        with:
          comment-id: ${{ github.event.comment.id }}
          reactions: 'rocket'
