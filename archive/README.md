# Archive — post-defense cleanup (2026-06-16)

Non-essential files moved here after the thesis defense. Nothing deleted — all recoverable.

- `decks/`   — redundant/superseded deck copies. The PRESENTED deck stays in `docs/ACR-QA_Defense_PRESENTED.{odp,pptx}`; rebuild a fresh source deck anytime with `scripts/build_template_deck.py`.
- `scripts/` — superseded deck builders (`build_defense_deck.py`, `build_custom_defense_deck.py`). CHANGELOG marks `build_defense_deck.py` "reference only". The live builder is `scripts/build_template_deck.py`.
- `html/`    — old generated HTML artifacts (banner/poster/Q1).
- `misc/`    — regenerable exports (e.g. DEFENSE_DECK.pdf — rebuild from the deck).
